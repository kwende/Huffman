#pragma once
#include <map>
#include <list>

using namespace std; 

template <class T>
class Huffman
{
public:
	Huffman();
	~Huffman();

	//Compress(list<T> input, char* outputBuffer, 
	//	int outputBufferLength, int* compressedLengthInBits); 
};

